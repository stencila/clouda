{
  "name": "stencila-cloud",
  "version": "0.27.0",
  "description": "Stencila in the cloud",
  "scripts": {
    "lint": "standard",
    "start": "if [ \"$NODE_ENV\" = \"development\" ]; then nodemon lib/serve.js; else node lib/serve.js; fi",
    "deps-uptodate": "ncu",
    "deps-used": "dependency-check --entry lib/serve.js . && dependency-check --extra --no-dev --entry lib/serve.js ."
  },
  "repository": "https://github.com/stencila/cloud",
  "license": "Apache-2.0",
  "devDependencies": {
    "dependency-check": "^3.1.0",
    "nodemon": "^1.17.5",
    "npm-check-updates": "^2.14.2",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "cookie": "^0.3.1",
    "dockerode": "^2.5.5",
    "jsonwebtoken": "^8.3.0",
    "koa": "^2.5.1",
    "koa-router": "^7.4.0",
    "kubernetes-client": "^5.3.0",
    "moniker": "^0.1.2",
    "pino": "^4.17.3",
    "random-port": "^0.1.0",
    "raw-body": "^2.3.3",
    "retry-request": "^4.0.0"
  }
}
